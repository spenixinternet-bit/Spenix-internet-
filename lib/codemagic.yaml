workflows:
  build:
    name: Build Spenix Android
    scripts:
      - flutter create .
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
